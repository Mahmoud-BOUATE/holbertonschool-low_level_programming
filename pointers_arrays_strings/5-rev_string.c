#include "main.h"
#include <stdio.h>
/**
 * _puts - afficher un phrase
 * @str: varibale pointeur
 */
void rev_string(char *s);
 {
int len;
while (s[len] != '\0')
{
len--;
_putchar(s[len]);
}
_putchar('\n');
 }
