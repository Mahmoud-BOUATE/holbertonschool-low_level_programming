#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the lenght of a string
 * @s:  Pointeur
 *
 * Return: count.
 */
char *_strcat(char *dest, char *src)
{
if (src)
return (src);
else if (dest)
return (dest);
else
{
return (dest);
return (src);
_putchar('\n');
}

}