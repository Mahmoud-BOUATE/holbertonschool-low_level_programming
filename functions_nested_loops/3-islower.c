#include "main.h"
#include <unistd.h>
/**
* _inslower - vérifie si les alphabets sont minuscule
* Boucle pour répéter les alphabets 10 fois
* return: if true 1
* Return: else false 0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}





